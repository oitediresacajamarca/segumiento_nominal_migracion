import { SeguimientoNominalEntity } from './seguimiento-nominal.entity';

describe('SeguimientoNominalEntity', () => {
  it('should be defined', () => {
    expect(new SeguimientoNominalEntity()).toBeDefined();
  });
});
